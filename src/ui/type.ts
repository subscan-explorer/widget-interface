export type MediaQueries = {
  xs: string;
  sm: string;
  md: string;
  lg: string;
  xl: string;
  xxl: string;
  nav: string;
};

export type ChainColors = {
  color?: string;
  borderRadius?: string;
};

export type Radii = {
  small: string;
  default: string;
  card: string;
  circle: string;
};
